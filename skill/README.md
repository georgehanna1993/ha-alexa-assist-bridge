# Alexa Skill Assets

This directory contains Alexa Skill assets that users copy into the Alexa Developer Console or deploy with the ASK CLI.

Current files:

- `interaction-model/en-US.json`

The skill endpoint is not implemented yet. The next development step is deciding and implementing the secure endpoint path.
